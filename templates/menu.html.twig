<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
    <a class="navbar-brand" href="{{ path('app_index') }}">
        <img class="mb-4" src="{{ asset('build/logo.png') }}" alt="" width="64" height="64">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_dashboard') }}">{{ 'link.dashboard'|trans }}</a>
                </li>
            {% endif %}
        </ul>

        <ul class="navbar-nav">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('app_logout') }}" class="btn btn-outline-light mr-2">{{ 'btn.logout'|trans }}</a>
            {% else %}
                <li>
                    <a href="{{ path('app_register') }}"
                       class="btn btn-outline-light mr-2">{{ 'btn.register'|trans }}</a>
                </li>
                <li>
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light">{{ 'btn.login'|trans }}</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>